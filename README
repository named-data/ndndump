This is the packet capture and analysis tool for Named Data Networking (NDN).
It is created to provide a tcpdump-like tool for NDN network.
For more information about NDN, click http://www.named-data.net/

To install:

$ ./configure
$ make
$ make install

if that failed, perhaps you need to generate 'configure' file by yourself.
$ ./autogen.sh
